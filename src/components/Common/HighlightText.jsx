import React from "react";

const HighlightText = ({ text }) => {
  return <span className="font-bold   
  bg-gradient-to-r from-blue-300 via-blue-100 via-65% to-caribbeangreen-50 text-transparent bg-clip-text"> {text}</span>;
};

export default HighlightText;
